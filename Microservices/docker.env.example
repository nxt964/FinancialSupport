# =============================================================================
# FINANCIAL SUPPORT SYSTEM - DOCKER ENVIRONMENT CONFIGURATION
# =============================================================================
# 
# HƯỚNG DẪN SỬ DỤNG:
# 1. Copy file này thành docker.env: cp docker.env.example docker.env
# 2. Điều chỉnh các giá trị bên dưới theo môi trường của bạn
# 3. KHÔNG BAO GIỜ commit file docker.env vào git repository
# 4. Thay đổi tất cả mật khẩu và secret keys mặc định
#
# =============================================================================

# ===== INFRASTRUCTURE SERVICES PORTS =====
# Các port cho infrastructure services (có thể thay đổi nếu bị trùng)
ZOOKEEPER_PORT=2181          # Kafka coordination service
KAFKA_PORT=9092              # Kafka message broker
KAFKA_UI_PORT=8080           # Kafka management web interface
REDIS_PORT=6379              # Redis cache & session store
POSTGRES_PORT=5432           # PostgreSQL database

# ===== API GATEWAY =====
# Main entry point for all client requests
# API_GATEWAY_HTTPS_PORT=5001  # Uncomment để enable HTTPS (cần certificate)
API_GATEWAY_HTTP_PORT=5000    # HTTP port (recommended for development)

# ===== USER SERVICE =====
# User management, authentication, and authorization
USER_SERVICE_HTTPS_PORT=44568 # HTTPS port (nếu enable)
USER_SERVICE_HTTP_PORT=5002   # HTTP port
JWT_SECRET=CHANGE_ME_TO_A_VERY_LONG_RANDOM_STRING_AT_LEAST_32_CHARACTERS  # BẮT BUỘC THAY ĐỔI

# ===== EMAIL SERVICE =====
# Email sending functionality for notifications
EMAIL_SERVICE_HTTPS_PORT=44567 # HTTPS port (nếu enable)
EMAIL_SERVICE_HTTP_PORT=5003   # HTTP port

# ===== CHART SERVICE =====
# Real-time chart data and market information
CHART_SERVICE_HTTPS_PORT=7114  # HTTPS port (nếu enable)
CHART_SERVICE_HTTP_PORT=5004   # HTTP port

# ===== NEWS SERVICE =====
# News fetching and management service
NEWS_SERVICE_HTTPS_PORT=44569  # HTTPS port (nếu enable)
NEWS_SERVICE_HTTP_PORT=5005    # HTTP port
NEWS_SERVICE_PORT=3000         # Internal service port

# ===== BACKTEST SERVICE =====
# Trading strategy backtesting functionality
BACKTEST_SERVICE_HTTP_PORT=7206  # HTTP port
BACKTEST_SERVICE_HTTPS_PORT=7207 # HTTPS port (nếu enable)

# ===== AI SERVICE =====
# AI analysis and machine learning services
AISERVICE_HTTP_PORT=8000   # HTTP port
AISERVICE_HTTPS_PORT=8001  # HTTPS port (nếu enable)

# ===== SENTIMENT SERVICE (OPTIONAL) =====
# Market sentiment analysis service
SENTIMENT_SERVICE_HTTPS_PORT=44570 # HTTPS port (nếu enable)
SENTIMENT_SERVICE_HTTP_PORT=5006   # HTTP port

# ===== DATABASE CONFIGURATION =====
# PostgreSQL database settings
POSTGRES_DB=UserServiceDb                    # Database name
POSTGRES_USER=postgres                       # Database username
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD       # BẮT BUỘC THAY ĐỔI - Database password

# ===== REDIS CONFIGURATION =====
# Redis cache and session store settings
REDIS_INSTANCE_NAME=UserService:             # Redis key prefix

# ===== EMAIL CONFIGURATION =====
# SMTP settings for email sending (Gmail example)
EMAIL_SMTP_HOST=smtp.gmail.com               # SMTP server hostname
EMAIL_SMTP_PORT=465                          # SMTP port (465 for SSL)
EMAIL_FROM_NAME=FinancialSupport             # Display name for sender
EMAIL_USERNAME=your-email@gmail.com          # BẮT BUỘC THAY ĐỔI - SMTP username
EMAIL_PASSWORD=your-app-password             # BẮT BUỘC THAY ĐỔI - SMTP password or app password
EMAIL_FROM_EMAIL=your-email@gmail.com        # BẮT BUỘC THAY ĐỔI - From email address
EMAIL_ENABLE_SSL=true                        # Enable SSL for SMTP

# ===== KAFKA CONFIGURATION =====
# Kafka message broker settings
KAFKA_BOOTSTRAP_SERVERS=kafka:29092          # Kafka broker address (internal Docker network)

# ===== BINANCE API CONFIGURATION =====
# Binance API for cryptocurrency data (OPTIONAL)
BINANCE_API_KEY=your-binance-api-key         # Binance API key (get from binance.com)
BINANCE_SECRET_KEY=your-binance-secret-key   # Binance secret key (get from binance.com)

# ===== ENVIRONMENT SETTINGS =====
# Application environment
ASPNETCORE_ENVIRONMENT=Production            # Development, Staging, or Production

# ===== API KEYS & TOKENS =====
# External service API keys (OPTIONAL)
EMAIL_SERVICE_API_KEY=your-email-service-api-key  # Email service API key
HF_TOKEN=your-huggingface-token                   # Hugging Face token for AI models

# ===== NEWS SERVICE DATABASE URLs =====
# Prisma database connection strings for News Service
# Sử dụng local PostgreSQL hoặc remote database
DATABASE_URL="postgresql://postgres:CHANGE_THIS_PASSWORD@postgres:5432/postgres"
DIRECT_URL="postgresql://postgres:CHANGE_THIS_PASSWORD@postgres:5432/postgres"

# ===== CRAWLING & CORS CONFIGURATION =====
# Web crawling and CORS settings
CRAWL_SITE_URL=https://cryptoslate.com/news/  # News source URL for crawling
CORS_ORIGINS=http://localhost:5173            # Allowed CORS origins (Frontend URL)

# =============================================================================
# QUAN TRỌNG - CÁC BƯỚC BẮT BUỘC:
# =============================================================================
# 
# 1. THAY ĐỔI MẬT KHẨU:
#    - POSTGRES_PASSWORD: Đặt mật khẩu mạnh cho database
#    - JWT_SECRET: Tạo chuỗi ngẫu nhiên dài ít nhất 32 ký tự
# 
# 2. CẤU HÌNH EMAIL (nếu cần gửi email):
#    - EMAIL_USERNAME: Email của bạn
#    - EMAIL_PASSWORD: App password (không phải mật khẩu thường)
#    - EMAIL_FROM_EMAIL: Email gửi đi
# 
# 3. API KEYS (tùy chọn):
#    - BINANCE_API_KEY & BINANCE_SECRET_KEY: Để lấy dữ liệu crypto
#    - HF_TOKEN: Để sử dụng AI models
# 
# 4. PORTS:
#    - Thay đổi nếu port bị trùng trên máy của bạn
#    - Kiểm tra bằng: netstat -an | grep :PORT_NUMBER
# 
# 5. KHÔNG BAO GIỜ:
#    - Commit file docker.env vào git
#    - Chia sẻ file docker.env với người khác
#    - Sử dụng mật khẩu mặc định trong production
# 
# =============================================================================

